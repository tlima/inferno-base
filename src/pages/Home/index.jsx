/* eslint-disable camelcase */
import Counter from 'components/Counter';

const Home = () => (
  <div>
    <div>Landing page - Build #{__webpack_hash__}</div>
    <Counter />
  </div>
);

export default Home;
